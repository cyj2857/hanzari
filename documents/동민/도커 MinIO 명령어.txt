MinIO

docker run -p 9000:9000 --name minio1 -v D:\data:/data -e "MINIO_ACCESS_KEY=AKIAIOSFODNN7EXAMPLE" -e "MINIO_SECRET_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY" minio/minio server /data

docker run -p 9001:9001 --name minio_spare -v D:\data:
key의 의미는
minio에
접속할 수 있는
선택한사람만 접근할 수 있게

액세스키, 시크릿키 - 서버를 올릴때 임의로 쓰고
레퍼런스

위의 액세스키, 시크릿키를 Node.js로 sdk를 이용해 요청을 보낼때 사용한다!!
레퍼런스

docker container exec -it minio1 bash